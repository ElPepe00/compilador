// Declaració de la funció 'sumar'
funcio int sumar(int a, int b):
    int res;
    res = a + b;
    return res;
funcio_fi

programa:

    int x = 15;
    imprimir(x);

    int y = llegir();
    
    // El resultat es guarda en una variable local
    int total = sumar(x, y);
    
    // Estructura condicional emprant el resultat
    if (total > 20):
        // Si és major que 20, imprimim true
        imprimir(true);
    else:
        // Si no, imprimim false
        imprimir(false);
    else_fi
programa_fi